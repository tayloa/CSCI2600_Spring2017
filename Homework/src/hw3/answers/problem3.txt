1.
public method division(p)
    input: another polynomial
    output: r = this/p 
	if p is 0 or this is NaN: return NaN
	if this is 0: return ZERO
	else:
		new_degree = (this.degree - p.degree)
		r = new RatNum[new_degree+1]
		fill r with RatNums equal to 0
		while i < this.size:
		  while j < p.size:
		     if (i-j >=0):
		     	r[i-j] = r[i-jj.add(this[i]/p[j])
		return RatPoly(r)
			
	